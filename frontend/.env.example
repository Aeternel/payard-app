# Server-only backend URL.
# Local host dev: http://localhost:8001/api/v1
# In the Caddy/compose stack the frontend service sets this to the internal
# http://backend:8000/api/v1 automatically (see compose.yaml).
INTERNAL_API_URL=http://localhost:8001/api/v1
